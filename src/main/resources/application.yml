server:
  port: 1234
  address: 0.0.0.0

spring:
  config:
    import: optional:classpath:application-secrets.yml
  datasource:
    url: jdbc:mysql://localhost:3306/astrodb?createDatabaseIfNotExist=true
    username: root
    password: root

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${SMTP_USERNAME:adhyaystore@gmail.com}
    password: ${SMTP_PASSWORD:ljpy pecz oclj wuex}
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

springdoc:
  swagger-ui:
    path: /swagger-ui.
    defaultModelsExpandDepth: -1
    docExpansion: none
  default-produces-media-type: application/json
  default-consumes-media-type: application/json
  api-docs:
    enabled: true
  model-and-view-allowed: false
  override-with-generic-response: false
  auto-tag-classes: false
otp:
  expiry-minutes: 5

textlocal:
  api-key: "YOUR_TEXTLOCAL_API_KEY"
  sender: "TXTLCL"

twofactor:
  enabled: true
  base-url: "https://2factor.in"
  api-key: "210c5d7a-0d79-11ec-a13b-0200cd936042"
  otp-template-name: "AstroAdhyaay"

resend:
  api-key: "re_CCseECjB_Bv36rHa5RYTMWJwyjnQtd8j6"
  from-email: "onboarding@resend.dev"

app:
  mail:
    from: "adhyaystore@gmail.com"
  public-base-url: ${APP_PUBLIC_BASE_URL:http://localhost:1234}

razorpay:
  key-id: "rzp_live_SGnLtGTl5vgLLC"
  key-secret: "snhHxD0UbGEx6fBhCZ9N9rHo"

free-astrology:
  base-url: "https://json.freeastrologyapi.com"
  api-key: "64mIJQUBn47P6znPRzWa52hMvu6fPKuy7DJycxS6"

fcm:
  enabled: true
  project-id: ${FCM_PROJECT_ID:}
  service-account-file: ${FCM_SERVICE_ACCOUNT_FILE:classpath:firebase-service-account.json}
  service-account-json: ${FCM_SERVICE_ACCOUNT_JSON:}
  service-account-base64: ${FCM_SERVICE_ACCOUNT_BASE64:}

agora:
  app-id: ${AGORA_APP_ID:}
  app-certificate: ${AGORA_APP_CERTIFICATE:}
  token-expire-seconds: ${AGORA_TOKEN_EXPIRE_SECONDS:3600}

google:
  maps:
    api-key: ${GOOGLE_MAPS_API_KEY:}
    places-autocomplete-url: ${GOOGLE_PLACES_AUTOCOMPLETE_URL:https://maps.googleapis.com/maps/api/place/autocomplete/json}
    places-details-url: ${GOOGLE_PLACES_DETAILS_URL:https://maps.googleapis.com/maps/api/place/details/json}
    geocode-url: ${GOOGLE_GEOCODE_URL:https://maps.googleapis.com/maps/api/geocode/json}
